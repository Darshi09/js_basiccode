// Write a JavaScript code snippet to compare two strings, "apple" and "banana", using comparison operators to check if the first string is less than the second string. Display the result using console.log.

let a = "apple"
let b = "banana"

if(a.length < b.length){
    console.log(`${a} string is less than the ${b} `)
}else{
    console.log(`${a} string is greater than the ${b} `)
}

// apple string is less than the banana