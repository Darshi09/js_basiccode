// Write a JavaScript code snippet that checks if a given variable age is either less than 18 or greater than 60 using logical operators, and displays the message "Not eligible" using console.log. If neither condition is met, display "Eligible".

let age = 65;

if (age < 18 || age > 60) {
    console.log("Not eligible");
} else {
    console.log("Eligible");
}

// Not eligible