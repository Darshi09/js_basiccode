// Given age = 25 and hasDrivingLicense = true, use logical operators to write a condition that checks if the person is eligible to drive. Print the result using console.log.

let age = 25;
let hasDrivingLicense = true;

if (age >= 18 && hasDrivingLicense) {
    console.log("The person is eligible to drive.");
} else {
    console.log("The person is not eligible to drive.");
}

// The person is eligible to drive.